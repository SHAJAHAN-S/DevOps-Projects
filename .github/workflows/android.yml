name: Android CI

# --------------------------------------------------
# 1️⃣ Triggers
# --------------------------------------------------
on:
  push:
    branches:
      - main
      - qa
      - develop
  pull_request:
    branches:
      - main
      - qa

# --------------------------------------------------
# 2️⃣ Workflow-level Environment Variables
# --------------------------------------------------
env:
  AWS_DEFAULT_REGION: ap-south-1
  JAVA_VERSION: 17

# --------------------------------------------------
# 3️⃣ Jobs
# --------------------------------------------------
jobs:
  build:
    name: Android Build & Analysis
    runs-on: ubuntu-latest

    steps:
      # --------------------------------------------
      # Step 1: Checkout source code
      # --------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v3

      # --------------------------------------------
      # Step 2: Setup Java
      # --------------------------------------------
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      # --------------------------------------------
      # Step 3: Cache Gradle
      # --------------------------------------------
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}

      # --------------------------------------------
      # Step 4: Grant execute permission
      # --------------------------------------------
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # --------------------------------------------
      # Step 5: Clean project
      # --------------------------------------------
      - name: Clean project
        run: ./gradlew clean

      # --------------------------------------------
      # Step 6: Run Android Lint
      # --------------------------------------------
      - name: Run Android Lint
        run: ./gradlew lint

      # --------------------------------------------
      # Step 7: Build project
      # --------------------------------------------
      - name: Build Android project
        run: ./gradlew build

      # --------------------------------------------
      # Step 8: SonarQube Scan
      # --------------------------------------------
      - name: SonarQube Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          ./gradlew sonarqube \
          -Dsonar.projectKey=android-demo \
          -Dsonar.projectName=android-demo \
          -Dsonar.host.url=$SONAR_HOST_URL \
          -Dsonar.login=$SONAR_TOKEN

      # --------------------------------------------
      # Step 9: Upload APK as artifact
      # --------------------------------------------
      - name: Upload Debug APK
        uses: actions/upload-artifact@v3
        with:
          name: debug-apk
          path: app/build/outputs/apk/debug/*.apk
